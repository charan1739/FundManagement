import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('App launches', (WidgetTester tester) async {
    // Placeholder — integration tests require full backend
    expect(true, isTrue);
  });
}
