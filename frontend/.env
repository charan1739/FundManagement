# Backend URL for the Vite dev proxy.
# Set this to your local backend when running it locally.
# If this is removed or left blank, the proxy will fall back to the deployed Render backend.
VITE_BACKEND_URL=http://localhost:5000
